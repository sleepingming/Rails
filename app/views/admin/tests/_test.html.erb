<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.questions.count %></td>

  <td><%= link_to 'Перейти к тесту', admin_test_path(test) %></td>
  <td>
    <%= link_to 'Удалить',
                admin_test_path(test),
                method: :delete,
                data: {confirm: 'Удалить тест?'} %>
  </td>
</tr>
